import 'package:flutter/material.dart';
import 'package:flutter/painting.dart';

class AppColors{

     static const Color PrimaryColor =   Color.fromARGB(255,98,0,238);
    static const Color onPrimary = Colors.white;
    static const Color onSecondary = Colors.black;

        static const Color onBackground = Colors.black;

  //  static const Color containerColor = Color.fromARGB(255, 200, 255, 200);


   static const Color backgroundColor =Colors.white;

    static const Color errorColor = Color.fromARGB(255, 176, 0, 32);
 static const Color  surfaceColor =  Colors.white;


 static const Color secondaryColor = Color.fromARGB(255, 3, 218, 198);


//vairants

static const Color primaryVariantColor2 = Color.fromARGB(255, 187,134,252);
 static const Color secondaryVariantColor = Color.fromARGB(255, 1, 135, 134);
 static const Color primaryVariantColor = Color.fromARGB(255, 55, 0, 179);

static const Color greenColor =  Color.fromARGB(255, 51, 156, 45);
  static const Color  whiteColor =  Colors.white;
  static const Color  blackColor =  Color(0xFF191B1C);

  /// Slightly lighter version of [backgroundColor].
  static const Color appBarColor = Color(0xFF191B1C);

  /// Color used for cards and surfaces.
  static const Color cardColor = Color(0xFF1F2426);

  /// Accent color used in the application.
  static const Color accentColor = Color(0xFFef8354);
}